package models;

public record Movie(String title,
                    int episode_id,
                    String director,
                    String producer,
                    String release_date) {
}
